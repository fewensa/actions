name: Clone Repository Workflow

on:
  push:
  workflow_dispatch:

jobs:

  clone-via-http:
    runs-on: ubuntu-latest
    steps:

      - name: Clone repository
        uses: ./clone-any
        with:
          repository: https://github.com/fewensa/actions

      - name: Access cloned repository content
        run: ls -l
